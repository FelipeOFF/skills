# A failed work item stops the forest

The next work item starts only when the requested stages of the current
one finished without halt. A child stacked on a red parent is a second
feature hidden in a broken review. Best-effort across siblings is a
later rule; v1 is serial, so halt is one rule. Print the tree: opened,
failed, still pending.
